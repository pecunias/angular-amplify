import { DynamicExampleComponentDirective } from './dynamic-example-component.directive';

describe('DynamicExampleComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicExampleComponentDirective();
    expect(directive).toBeTruthy();
  });
});
